<mvc:View
	controllerName="unomi.ui.controller.DefinitionCatalog"
	xmlns:mvc="sap.ui.core.mvc"
	xmlns="sap.m"
	displayBlock="true">
	<Page title="{i18n>definitions}" busy="{cat>/busy}" busyIndicatorDelay="0">
		<IconTabBar expandable="false">
			<items>
				<IconTabFilter text="{i18n>conditions} ({cat>/conditions/length})" key="conditions">
					<List items="{cat>/conditions}" noDataText="{i18n>noData}">
						<StandardListItem title="{cat>name}" description="{cat>id}" info="{cat>version}" />
					</List>
				</IconTabFilter>
				<IconTabFilter text="{i18n>actions} ({cat>/actions/length})" key="actions">
					<List items="{cat>/actions}" noDataText="{i18n>noData}">
						<StandardListItem title="{cat>name}" description="{cat>id}" info="{cat>version}" />
					</List>
				</IconTabFilter>
			</items>
		</IconTabBar>
	</Page>
</mvc:View>
