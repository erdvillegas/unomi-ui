# Spanish. The base bundle (i18n.properties) is already Spanish and is the
# default/fallback, so this file only needs to exist for an explicit `es`
# request; it inherits every key from the base. Add overrides here only if the
# base ever switches to another default language.
